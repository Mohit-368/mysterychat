import { useState } from "react";
import { Link, useNavigate } from "react-router-dom";
import { useAuth } from "../context";

export default function Navbar() { const [open, setOpen] = useState(false); const { user, logout } = useAuth(); const navigate = useNavigate(); const signOut = async () => { await logout(); navigate("/"); setOpen(false); }; return <nav className="fixed top-0 w-full z-40 bg-black/70 backdrop-blur-xl border-b border-white/5"><div className="max-w-7xl mx-auto px-6 h-20 flex items-center justify-between"><Link to="/" className="text-xl font-semibold text-white">Mystery<span className="text-blue-500 font-light">Chat</span></Link><div className="hidden md:flex items-center gap-8 text-sm text-gray-400"><Link to="/">Home</Link><Link to="/about">About</Link>{user && <Link to="/chat" className="text-blue-400">Rooms</Link>}</div><div className="hidden md:flex items-center gap-5">{user ? <><span className="text-sm text-gray-500">@{user.username}</span><button onClick={signOut} className="text-sm text-gray-300">Logout</button></> : <><Link to="/login" className="text-sm text-gray-300">Login</Link><Link to="/register" className="text-sm bg-white text-black px-5 py-2.5 rounded-full">Register</Link></>} </div><button className="md:hidden text-gray-300" onClick={() => setOpen(!open)} aria-label="Menu">{open ? "✕" : "☰"}</button></div>{open && <div className="md:hidden bg-black border-t border-white/5 px-6 py-5 space-y-4"><Link className="block" to="/" onClick={() => setOpen(false)}>Home</Link><Link className="block" to="/about" onClick={() => setOpen(false)}>About</Link>{user ? <><Link className="block text-blue-400" to="/chat" onClick={() => setOpen(false)}>Rooms</Link><button onClick={signOut}>Logout</button></> : <><Link className="block" to="/login" onClick={() => setOpen(false)}>Login</Link><Link className="block" to="/register" onClick={() => setOpen(false)}>Register</Link></>}</div>}</nav>; }
